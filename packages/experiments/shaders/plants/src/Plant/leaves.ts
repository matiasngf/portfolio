import { PathVertices } from "./helpers/path-vertex";

export interface LeaveUniforms {
  leaveProgress: number;
}

export const getLeaveMesh = (path: PathVertices, leaveUniforms: LeaveUniforms) => {
  // const leaveGeometry
}